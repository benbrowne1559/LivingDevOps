# 2 Tier Static App on Docker

Transforms the previous project 'EC2WebApp' into a docker image with docker compose

1. Created a DockerFile to install dependencies for flask app and run it
2. Create docker-compose.yml file that configures the app + database and makes the app dependant on the db with a health check


